/* Main Screen Styles */

MainScreen {
    background: transparent;
}

#title {
    padding: 1;
    background: transparent;
    text-align: center;
}

#gantt-title, #table-title {
    padding: 1 0;
    text-align: left;
    background: transparent;
}

#gantt-widget {
    width: 100%;
    height: auto;
    max-height: 50;
    border: solid $primary;
    margin-bottom: 1;
    overflow-y: auto;
    overflow-x: auto;
}

#task-table {
    height: auto;
    min-height: 10;
    border: solid $primary;
}
